description: Expected response to a valid request
content:
  application/json:
    schema:
      allOf:
        - $ref: "../../index.yaml#/components/schemas/cCollection"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "../../index.yaml#/components/schemas/Organization"
