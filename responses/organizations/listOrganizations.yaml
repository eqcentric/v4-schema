description: Expected response to a valid request
content:
  application/json:
    schema:
      allOf:
        - $ref: "#/components/schemas/cCollection"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/Organization"
